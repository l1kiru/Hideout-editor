# Frontend

This is the React/Vite frontend for Hideout Editor.

Most users should start the app from the repository root:

```bash
python dev.py
```

Frontend-only commands, for development:

```bash
npm install
npm run dev
npm run lint
npm run build
```

See the root `README.md` for installation and update instructions.
